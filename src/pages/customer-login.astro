---
import Base from "../layouts/Base.astro";
---
<Base
  title="Customer Login | Cusoon Pools"
  description="Secure customer portal for Cusoon Pools clients."
  canonical="https://cusoon-pools.com/customer-login/"
>
  <section class="min-h-[70vh] bg-[radial-gradient(circle_at_top,_rgba(96,200,215,0.25),_transparent_60%)] pt-6 pb-12">
    <div class="container mx-auto flex max-w-2xl flex-col gap-10 rounded-[32px] border border-sand bg-white/90 p-10 shadow-2xl">
      <div class="space-y-3 text-center">
        <p class="text-sm font-medium uppercase tracking-[0.3em] text-ink/60 sm:text-lg">Customer Portal</p>
        <h1 class="mt-2 font-display text-2xl sm:text-3xl">Sign in to your Cusoon Pools account</h1>
        <p class="text-sm text-ink/70">
          Access reports, service history, and schedule updates. Use the form below to continue.
        </p>
      </div>
      <form class="space-y-6 text-sm">
        <label class="block">
          <span class="text-ink/70">Email</span>
          <input
            type="email"
            placeholder="you@email.com"
            required
            class="mt-2 w-full rounded-xl border border-ink/20 px-4 py-3 text-ink placeholder:text-ink/50 focus:border-sea focus:outline-none focus:ring-2 focus:ring-aqua/60"
          />
        </label>
        <label class="block">
          <span class="text-ink/70">Password</span>
          <div class="relative mt-2">
            <input
              id="login-password"
              type="password"
              placeholder="Enter your password"
              required
              class="w-full rounded-xl border border-ink/20 px-4 py-3 pr-14 text-ink placeholder:text-ink/50 focus:border-sea focus:outline-none focus:ring-2 focus:ring-aqua/60"
            />
            <button
              type="button"
              data-password-toggle
              class="absolute inset-y-0 right-2 flex items-center rounded-full border border-ink/10 px-3 text-xs uppercase tracking-[0.2em] text-ink/70 transition hover:border-ink/40 hover:text-ink"
            >
              Show
            </button>
          </div>
        </label>
        <div class="flex flex-wrap items-center justify-between gap-4">
          <button
            type="button"
            class="w-full rounded-full bg-sea px-6 py-3 text-sm font-semibold uppercase tracking-[0.25em] text-white transition hover:bg-ink sm:w-auto"
          >
            Sign in
          </button>
          <button
            type="button"
            class="w-full rounded-full border border-ink/20 px-6 py-3 text-sm font-semibold uppercase tracking-[0.25em] text-ink transition hover:border-sea hover:text-sea sm:w-auto"
          >
            Create account
          </button>
        </div>
        <p class="text-center text-xs uppercase tracking-[0.3em] text-ink/50">
          <a href="/contact" class="hover:text-sea">Forgot your password?</a>
        </p>
      </form>
    </div>
  </section>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const button = document.querySelector("[data-password-toggle]");
      if (!button) return;
      const passwordInput = document.querySelector("#login-password");
      button.addEventListener("click", () => {
        if (passwordInput.type === "password") {
          passwordInput.type = "text";
          button.textContent = "Hide";
        } else {
          passwordInput.type = "password";
          button.textContent = "Show";
        }
      });
    });
  </script>
</Base>
