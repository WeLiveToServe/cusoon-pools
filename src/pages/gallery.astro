---
import Base from "../layouts/Base.astro";
import { site } from "../data/site";
---
<Base
  title="Gallery | Cusoon Pools"
  description="Before-and-after gallery from Cusoon Pools' cleaning, repair, and seasonal service jobs."
  canonical="https://cusoon-pools.com/gallery/"
>
  <section class="pt-6 pb-12">
    <div class="container">
      <div class="mx-auto max-w-4xl text-center">
        <p class="text-sm font-medium uppercase tracking-[0.3em] text-ink/60 sm:text-lg">Gallery</p>
        <h1 class="mt-2 font-display text-2xl sm:text-3xl">Pool Service Gallery</h1>
        {false && <p class="mt-6 text-ink/70">These are a few of our favorite pools.</p>}
      </div>
      <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {site.galleryImages.map((imageUrl, index) => (
          <div class="group relative overflow-hidden rounded-3xl border border-sand shadow-lift">
            <img
              src={imageUrl}
              alt={`Cusoon Pools project ${index + 1}`}
              class="h-72 w-full object-cover transition duration-500 group-hover:scale-105"
              loading="lazy"
            />
          </div>
        ))}
      </div>
      <p class="mt-10 text-center text-sm text-ink/70">
        We leave your pool beautiful, each and every time.
      </p>
    </div>
  </section>
</Base>
